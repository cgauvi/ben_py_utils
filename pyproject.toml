[tool.poetry]
name = "ben-py-utils"
version = "0.1.0"
description = ""
authors = ["Charles Gauvin <you@example.com>"]
readme = "README.md"
packages = [{include = "ben_py_utils"}]

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
